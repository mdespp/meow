# meow

MeowSquad care log React app.

## Run locally

```bash
npm install
npm run dev
```

## Push to GitHub

Open this folder in VS Code, commit it, then click **Publish Branch**.

For GitHub Pages, go to your repo settings:

**Settings → Pages → Source → GitHub Actions**

Then push to `main`. GitHub will build and deploy the site automatically.
